CREATE DATABASE '<projeto>/DB/AVALIACAO.FDB' USER 'SYSDBA' PASSWORD '<senha>'

CREATE TABLE CLIENTE (
	CD_CODIGOCLI INTEGER NOT NULL,
	DS_NOME VARCHAR(60),
	DS_ENDERECO VARCHAR(60),
	DS_BAIRRO VARCHAR(30),
	DS_CIDADE VARCHAR(60),
	DS_UF VARCHAR(2),
	DS_FONE VARCHAR(20),
	NR_CEP VARCHAR(9),
	CONSTRAINT INTEG_2 PRIMARY KEY (CD_CODIGOCLI)
);
CREATE UNIQUE INDEX RDB$PRIMARY1 ON CLIENTE (CD_CODIGOCLI);